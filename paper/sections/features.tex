%% ============================================================================
\section{Feature Analysis}
\label{sec:features}
%% ============================================================================

\subsection{Correlation with $\kappa$}
\label{subsec:raw_spearman}

Table~\ref{tab:raw_spearman} lists the top ten features for each sensor ranked by
$|\rho(\text{feature},\, \kappa)|$ across all 799~sweeps per sensor.

\begin{table}[H]
\centering
\caption{Top 10 features by combined Spearman--Pearson rank score with $\kappa$,
for the AE channel (all sub-bands and broadband combined) and the UL channel.
After the Stage~2 redundancy reduction, 32~AE and 6~UL features are retained
for modelling; the retained sets are dominated by sub-band features for AE and
amplitude/spectral features for UL.}
\label{tab:raw_spearman}
\small
\begin{tabular}{lccl cc}
\toprule
\multicolumn{3}{c}{\textbf{AE (best feature per rank)}} &
\phantom{x} &
\multicolumn{2}{c}{\textbf{UL}} \\
\cmidrule(lr){1-3}\cmidrule(lr){5-6}
Feature & $|\rho|$ & $|r|$ & & Feature & $|\rho|$ \\
\midrule
1--2~MHz complexity         & 0.891 & 0.736 & & Spectral mean     & 0.663 \\
500k--1~MHz mobility        & 0.841 & 0.663 & & Peak              & 0.629 \\
500k--1~MHz centre freq.    & 0.789 & 0.687 & & RMS               & 0.607 \\
500k--1~MHz freq.-wt.\ std  & 0.789 & 0.686 & & Std               & 0.606 \\
500k--1~MHz norm.\ freq.\ std & 0.789 & 0.679 & & Spectral std    & 0.607 \\
1--2~MHz shape factor       & 0.825 & 0.628 & & Variance          & 0.606 \\
1--2~MHz spectral mean      & 0.801 & 0.590 & & Norm.\ bandwidth  & 0.607 \\
1--2~MHz norm.\ bandwidth   & 0.782 & 0.599 & & Freq.\ skewness   & 0.539 \\
1--2~MHz std                & 0.777 & 0.552 & & Freq.\ kurtosis   & 0.537 \\
1--2~MHz RMS                & 0.777 & 0.552 & & Kurtosis          & 0.469 \\
\bottomrule
\end{tabular}
\end{table}

The top AE features are drawn overwhelmingly from the 1--2~MHz and 500~kHz--1~MHz
sub-bands, with sub-band complexity ($|\rho| = 0.891$) and mobility ($|\rho| = 0.841$)
heading the ranking.
Broadband AE features rank lower than their sub-band counterparts, indicating that
the frequency-specific content of the high-frequency AE signal carries more monotonic
information about $\kappa$ than the full-band aggregate.
For the UL channel, amplitude features (spectral mean, peak, RMS, std) dominate at
$|\rho| \approx 0.60$--$0.66$; the maximum UL correlation ($|\rho| = 0.663$) is
substantially lower than the top AE correlation ($|\rho| = 0.891$).
Figure~\ref{fig:ranking_ae} and Figure~\ref{fig:ranking_us} show the full feature
correlation rankings for each sensor.

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/feature_ranking_ae_barplot.png}
\caption{Spearman $|\rho|$ and Pearson $|r|$ between each AE feature and $\kappa$,
sorted by combined rank.
Retained features (after Stage~2 redundancy filtering) are highlighted.}
\label{fig:ranking_ae}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/feature_ranking_us_barplot.png}
\caption{Spearman $|\rho|$ and Pearson $|r|$ between each UL-channel feature and
$\kappa$, sorted by combined rank. Retained features highlighted.}
\label{fig:ranking_us}
\end{figure}

\subsection{Operating-condition confounding}
\label{subsec:confound}

The raw Spearman correlation between broadband signal energy (as a proxy for all amplitude
features) and $\kappa$ is substantially driven by the shared dependence on rotational speed
and temperature.
Quantitatively, RPM has a Spearman correlation of $\rho = +0.62$ with $\kappa$ and
temperature has $\rho = -0.68$.
Higher RPM reduces the required viscosity $\nureq$ (Eq.~\eqref{eq:nu1}) and therefore
increases $\kappa$, while simultaneously increasing signal energy through more frequent
contact events per second.
Higher temperature reduces the actual viscosity $\nuop$ and therefore reduces $\kappa$,
while also affecting material damping and structural dynamics.

As a consequence, amplitude-dominated features such as RMS, variance, and peak --- which
rank among the top features by raw Spearman correlation --- are partially acting as
indirect proxies for rotational speed rather than as direct indicators of lubrication
film adequacy.
This confounding is an inherent property of any single-operating-point sensor system
without an explicit operating condition normalisation step.

Spectral shape features (frequency kurtosis, normalised bandwidth, spectral flatness)
are less directly tied to total signal power and are therefore expected to carry a
higher proportion of genuine lubrication-regime information relative to their raw
$|\rho|$ rank.
The $\kappa$ regression framework partially addresses this by training models on the full
multi-condition dataset, where speed and temperature appear as independent axes of
variation and the model can implicitly learn to discriminate amplitude changes due to
lubrication from those due to operating conditions.

\subsection{PCA and regime separability}
\label{subsec:pca}

Figure~\ref{fig:pca} shows the first two principal components of the retained AE
feature set, coloured by $\kappa$ regime.
Boundary and full-film sweeps are broadly separated in the PC1--PC2 plane, while mixed
regime sweeps overlap with both.
This confirms that the retained feature set carries regime-relevant discriminative
information but that the mixed lubrication regime remains the most challenging region,
consistent with the known partial physical overlap between boundary and mixed conditions.

\begin{figure}[H]
\centering
\includegraphics[width=0.7\textwidth]{figures/pca_kappa_regimes.png}
\caption{PCA projection of the retained AE feature set, coloured by lubrication regime.
Boundary ($\kappa < 0.5$): red; mixed ($0.5 \leq \kappa < 1$): orange;
full film ($\kappa \geq 1$): green.}
\label{fig:pca}
\end{figure}
