%% ============================================================================
\section{Experimental Setup}
\label{sec:setup}
%% ============================================================================

\subsection{Test rig and bearing}
\label{subsec:rig}
\todo{Include picture of test rig}
The test rig is a custom-built bearing test stand at AU (Herning, Denmark). A single deep-groove ball bearing (model SYJ25, bore \SI{25}{mm}, pitch diameter $d_{pw} = \SI{38.0}{mm}$, 9 chrome steel rolling elements, dynamic load rating \SI{14000}{N}) is driven by a variable-frequency electric motor controlled through a variable-frequency drive (VFD). The bearing is loaded radially by a static weight and lubricated by a defined \todo{What is the amount?} quantity of Keratech~22 oil (kinematic viscosity $\nu_{40} = \SI{22}{cSt}$, $\nu_{100} = \SI{4.1}{cSt}$) \todo{Do we need NLGI cosistency grade?}. \todo{How is temperature controlled and measured?}

\textit{Note on VFD noise:} Harmonic artefacts from the VFD switching frequency are present at approximately \SI{5}{kHz}\todo{Check that this is correct} and its multiples in the low-frequency range of both sensor signals. These are partially filtered out \todo{Explain the process and expected outcome.}

\subsection{Operating conditions}
\label{subsec:ops}

The experiment follows a protocol which varies the operating conditions through changes in rotational speed (rpm) and housing temperature (\si{\celsius}). This consists of a staircase sweep from 100 to 3000 RPM and back down to 100 RPM in steps of 100 RPM with a 60-second hold at each step\todo{How is RPM controlled}. This, therefore, results in This is repeated for each of 13 temperature blocks (40–100°C in 5°C increments) where temperature is controlled with \todo{With what?}. Sweeps of rpm are separated with a 2-minute standstill (RPM = 0) at the start of each block for thermal stabilisation.

The lubrication conditions in the bearing are not expected to change during the experiment in other ways than what is entailed by rpm and temperature and the effect of these upon the lubricant. It is not expected or assumed that any degradation, starvation or contamination of the lubricant happens.

The operating conditions are controlled through \todo{How?} and the resulting measured operating conditions over time can be seen in Figure~\ref{fig:op_conditions}.\todo{Update figure with new op conds}

\begin{figure}[H]
\centering
\includegraphics[width=0.9\textwidth]{figures/eda_operating_conditions.png}
\caption{Caption}
\label{fig:op_conditions}
\end{figure}

\subsection{Sensor configuration and data acquisition}
\label{subsec:sensors}

Two sensors simultaneously acquire signals from the bearing housing through a shared oscilloscope which samples at a shared sample rate of \SI{12.5}{MHz}:

\begin{description}
  \item[Acoustic emission sensor:] A wideband piezoelectric AE sensor is bonded to the bearing housing. \todo{Need help from CeramicSpeed to write this}
  \item[Ultrasound sensor:] A heterodyned passive US probe is bonded to the bearing housing. \todo{Need help from CeramicSpeed to write this}
  \item[Temperature sensor:] \todo{Need help from CeramicSpeed to write this}
  \item[RPM sensor:] \todo{Need help from CeramicSpeed to write this}
\end{description}
The shared sample rate ensures that AE and US signals share timestamps.

\subsection{$\kappa$ derivation}
The $\kappa$ value for each sweep is computed according to Equations~\eqref{eq:kappa} and \eqref{eq:nu1} using the per-sweep measured RPM and temperature, with \nuop{} evaluated via the ASTM~D341 Walther viscosity--temperature equation. No separate reference measurement of film thickness is available\todo{Change this if the slipring is used}; $\kappa$ is therefore a model-derived quantity subject to the assumptions of the Hamrock--Dowson framework and Walther's equation.
\todo{Include plots of the kappa values and operating conditions.}
\subsection{Dataset availability}
\todo{Is the dataset proprietary or public?}
